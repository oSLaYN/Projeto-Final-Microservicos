apiVersion: apps/v1
kind: Deployment
metadata:
  name: maria-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: maria-db
  template:
    metadata:
      labels:
        app: maria-db
    spec:
      containers:
      - name: maria-db
        image: mariadb:10.11
        ports:
        - containerPort: 3306  # Alterado para a porta 3307
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "Kubernetes@"   # Senha de root do MariaDB
        - name: MYSQL_TCP_PORT
          value: "3306"   # Configurar a porta do MariaDB para 3307
        volumeMounts:
        - name: maria-db-persistent-storage
          mountPath: /var/lib/mysql  # Altere o caminho para /var/lib/mysql (padrão do MariaDB)
      volumes:
      - name: maria-db-persistent-storage
        persistentVolumeClaim:
          claimName: maria-db-pvc   # Nome do PVC para o armazenamento persistente
