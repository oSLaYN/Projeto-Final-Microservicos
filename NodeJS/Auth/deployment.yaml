apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-node
  namespace: auth-node
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-node
  template:
    metadata:
      labels:
        app: auth-node
    spec:
      containers:
      - name: auth-node
        image: node-app:1.0  # Será atualizado
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"